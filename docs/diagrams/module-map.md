# CAPS — Module Map
# كابس — خريطة الوحدات

```mermaid
graph LR
    subgraph CAPS
        Core["CAPS Core"]
        Settings["CAPS_ Settings"]
    end
    style Core fill:#10B981,color:#fff
```

> **Note:** This is a placeholder module map. Update with actual modules from `modules.txt`.
